let JWT_SECRET,jwt;_bcc‍.x([["default",()=>_bcc‍.o]]);_bcc‍.w("../config",[["JWT_SECRET",["JWT_SECRET"],function(v){JWT_SECRET=v}]]);_bcc‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);


class JwtService {
  static sign(payload, expiry = "60s", secret = JWT_SECRET) {
    return jwt.sign(payload, secret, { expiresIn: expiry });
  }
  static verify(token,secret=JWT_SECRET){
    return jwt.verify(token,secret);
  }
}

_bcc‍.d(JwtService);
