let Joi,CustomeErrorHandler,User,bcrypt,JwtService;_762‍.x([["default",()=>_762‍.o]]);_762‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_762‍.w("../../services/CustomeErrorHandling",[["default",["CustomeErrorHandler"],function(v){CustomeErrorHandler=v}]]);_762‍.w("../../models",[["User",["User"],function(v){User=v}]]);_762‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_762‍.w("../../services/JwtService",[["default",["JwtService"],function(v){JwtService=v}]]);





const registerController = {
  async register(req, res, next) {
    //===========Validation==============================//
    const registerSchema = Joi.object({
      name: Joi.string().min(3).max(30).required(),
      email: Joi.string().email().required(),
      password: Joi.string()
        .pattern(new RegExp("^[a-zA-Z0-9]{3,30}$"))
        .required(),
      repeat_password: Joi.ref("password"),
    });
    const { error } = registerSchema.validate(req.body);
    if (error) {
      return next(error); // passing the error to errorHandler.js
    }

    //==========check if user is in the database already=========//
    try {
      const exist = await User.exists({ email: req.body.email });
      if (exist) {
        return next(
          CustomeErrorHandler.alreadyExist("This email is already taken")
        );
      }
    } catch (error) {
      return next(error); // passing the error
    }

    //==Hash password==//
    const { name, email, password } = req.body;

    const hasedPassword = await bcrypt.hash(password, 10);

    //==prepare the model==//

    const user = new User({ name, email, password: hasedPassword });
    let access_token;
    try {
      const result = await user.save();

      //======Token=======//

      access_token = JwtService.sign({ _id: result._id, role: result.role });
    } catch (error) {
      return next(error);
    }

    res.json({ access_token: access_token });
  },
};

_762‍.d(registerController);

// * validate the request
// * authorise the request
// * check if user is in the database already
// * prepare model
// * store in database
// * generate jwt token
// * send response
