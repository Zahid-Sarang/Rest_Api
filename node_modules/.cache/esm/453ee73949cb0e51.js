let Product,multer,CustomeErrorHandler,path,Joi,fs,productSchema;_49c‍.x([["default",()=>_49c‍.o]]);_49c‍.w("../../models",[["Product",["Product"],function(v){Product=v}]]);_49c‍.w("multer",[["default",["multer"],function(v){multer=v}]]);_49c‍.w("../../services/CustomeErrorHandling",[["default",["CustomeErrorHandler"],function(v){CustomeErrorHandler=v}]]);_49c‍.w("path",[["default",["path"],function(v){path=v}]]);_49c‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_49c‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_49c‍.w("../../validators/productValidators",[["default",["productSchema"],function(v){productSchema=v}]]);







const storage = multer.diskStorage({
  destination: (req, file, cb) => cb(null, "uploads/"),
  filename: (req, file, cb) => {
    const uniqueName = `${Date.now()}-${Math.round(
      Math.random() * 1e9
    )}${path.extname(file.originalname)}`; // <= genrating uniqueName for file like 3453453-43434.png
    cb(null, uniqueName);
  },
});

const handleMultipartData = multer({
  storage,
  limits: { fileSize: 1000000 * 5 },
}).single("image"); // 5mb

const productController = {
  async store(req, res, next) {
    // Multipart form data
    handleMultipartData(req, res, async (err) => {
      if (err) {
        return next(CustomeErrorHandler.serverError(err.message));
      }
      const filePath = req.file.path;
      // validation

      const { error } = productSchema.validate(req.body);
      if (error) {
        // Delete the uploaded file
        fs.unlink(`${appRoot}/${filePath}`, (err) => {
          if (err) {
            return next(CustomeErrorHandler.serverError(err.message));
          }
        });

        return next(error);
        // rootfolder/uploads/filename.png
      }

      const { name, price, size } = req.body;
      let document;
      try {
        document = await Product.create({
          name,
          price,
          size,
          image: filePath,
        });
      } catch (error) {
        return next(error);
      }
      res.status(201).json(document);
    });
  },

  //=================================== Update Product ==========================================//
  async update(req, res, next) {
    handleMultipartData(req, res, async (err) => {
      if (err) {
        return next(CustomeErrorHandler.serverError(err.message));
      }
      let filePath;
      if (req.file) {
        filePath = req.file.path;
      }
      // validation

      const { error } = productSchema.validate(req.body);
      if (error) {
        if (req.file) {
          // Delete the uploaded file
          fs.unlink(`${appRoot}/${filePath}`, (err) => {
            if (err) {
              return next(CustomeErrorHandler.serverError(err.message));
            }
          });
        }

        return next(error);
        // rootfolder/uploads/filename.png
      }

      const { name, price, size } = req.body;
      let document;
      try {
        document = await Product.findOneAndUpdate(
          { _id: req.params.id },
          {
            name,
            price,
            size,
            ...(req.file && { image: filePath }),
          },
          { new: true }
        );
      } catch (error) {
        return next(error);
      }
      res.status(201).json(document);
    });
  },

  //=================================== Delete Product ==========================================//

  async destroy(req, res, next) {
    const document = await Product.findOneAndRemove({ _id: req.params.id });
    if (!document) {
      return next(new Error("Nothing to Delete"));
    }
    // image delete
    const imagePath = document.image;
    fs.unlink(`${appRoot}/${req.file.path}`, (err) => {
      if (err) {
        return next(CustomeErrorHandler.serverError(err.message));
      }
    });

    res.status(201).json(document);
  },
};

_49c‍.d(productController);
