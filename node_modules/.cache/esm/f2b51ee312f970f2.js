let Joi,bcrypt,User,CustomeErrorHandler,JwtService;_12f‍.x([["default",()=>_12f‍.o]]);_12f‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_12f‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_12f‍.w("../../models",[["User",["User"],function(v){User=v}]]);_12f‍.w("../../services/CustomeErrorHandling",[["default",["CustomeErrorHandler"],function(v){CustomeErrorHandler=v}]]);_12f‍.w("../../services/JwtService",[["default",["JwtService"],function(v){JwtService=v}]]);







const loginController = {
  async login(req, res, next) {
    // validation
    const loginSchema = Joi.object({
      email: Joi.string().email().required(),
      password: Joi.string()
        .pattern(new RegExp("^[a-zA-Z0-9]{3,30}$"))
        .required(),
    });
    const { error } = loginSchema.validate(req.body);
    if (error) {
      return next(error);
    }
    try {
      const user = await User.findOne({ email: req.body.email });
      if (!user) {
        return next(CustomeErrorHandler.wrongCredentials());
      }

      // Compare the password
      const match = await bcrypt.compare(req.body.password, user.password);
      if (!match) {
        return next(CustomeErrorHandler.wrongCredentials());
      }
      // Token genrate
      const access_token = JwtService.sign({
        _id: user._id,
        role: user.role,
      });
      res.json({access_token})
    } catch (err) {
      return next(err);
    }
  },
};

_12f‍.d(loginController);
